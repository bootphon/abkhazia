# Abkhazia-specific rules

# Allow only a readme in data directories
/ABXpy/*
!/ABXpy/README.txt
/kaldi/*
!/kaldi/README.txt
/corpora/*
!/corpora/README.txt

# In corpora folder allow corpus-specific bin folders 
# containing scripts and instructions to prepare
# everything that is needed from the raw corpus distribution
!/corpora/*/
/corpora/*/*
!/corpora/*/bin

# MacOS .DS_Store
*.DS_Store
# Some temporary files
*~

# Standard Python gitignore

# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
# C extensions
*.so
# Distribution / packaging
.Python
env/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
*.egg-info/
.installed.cfg
*.egg
# PyInstaller
# Usually these files are written by a python script from a template
# before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec
# Installer logs
pip-log.txt
pip-delete-this-directory.txt
# Unit test / coverage reports
htmlcov/
.tox/
.coverage
.cache
nosetests.xml
coverage.xml
# Translations
*.mo
*.pot
# Django stuff:
*.log
# Sphinx documentation
docs/_build/
# PyBuilder
target/
